<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Gimnasio Domrivas</title>
        
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>

        <style>

            {% block style %}
            {% endblock %}

            body {
                font-family: Arial, sans-serif;
                background-color: #f4f4f9;
            }
            
            .content {
                max-width: 1000px;
                margin: 2rem auto;
                background-color: #e5e5e5;
                padding: 2rem;
                border-radius: 10px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }
            .servicios {
                margin-top: 2rem;
            }
            .form-container {
                margin-bottom: 20px;
            }
            table {
                width: 100%;
                border-collapse: collapse;
            }
            th, td {
                padding: 10px;
                border: 1px solid #ddd;
            }
            th {
                background-color: #f4f4f4;
            }

            .btn-primary, .btn-primary:hover, .btn-primary:active {
                background-color: #6b9080;
                border-color: #6b9080;
            }

            .login-form h1 {
                margin-bottom: 1rem;
            }

            .login-form input {
                display: block;
                width: 100%;
                padding: 0.5rem;
                margin: 0.5rem 0;
                border: 1px solid #ccc;
                border-radius: 5px;
            }
            .login-form button {
                background-color: #333;
                color: white;
                padding: 0.5rem;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }
            .login-form button:hover {
                background-color: #555;
            }
            .ficha-item {
                margin-bottom: 10px;
            }
            .ficha-item strong {
                display: inline-block;
                width: 100px;
                color: #555;
            }


        </style>
    </head>
    <body>

        <nav class="navbar navbar-inverse">
            <div class="container-fluid">
                <div class="navbar-header">
                <a class="navbar-brand" href="#">Gimnasio Domrivas</a>
                </div>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('inicio') }}" >Inicio</a></li>
                    {% if app.user %}
                        <li><a href="{{ path('logout') }}">Cerrar Sesión</a></li>
                    {% else %}
                        <li><a href="{{ path('login') }}">Iniciar Sesión</a></li>
                        <li><a href="{{ path('registro') }}">Registrate</a></li>
                    {% endif %}
                    <li><a href="{{ path('reservas') }}">Reservas</a></li>
                    {% if is_granted('ROLE_ADMINISTRADOR') %}
                        <li><a href="{{ path('administrador') }}">Administrador</a></li>
                    {% endif %}
                </ul>
            </div>
        </nav>
        <div class="content">
            {% block body %}
            {% endblock %}
        </div>

        <footer class="py-3 my-4">
            <div class="row">
                <p class="text-center text-muted">Gimnasio Domrivas - ¡Tu lugar para mantenerte en forma!</p>
                <p class="text-center text-muted">Calle San Rafael 25 - Coria (Cáceres)</p>
                <p class="text-center text-muted">Horario: 06:00 - 24:00, abierto 365 dias al año</p>
            </div>
        </footer>
    </body>
</html>
